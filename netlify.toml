[build]
  # Static site, no real build — just assemble the servable files into dist/.
  # Publishing dist/ keeps us compatible with the dashboard's publish dir.
  command = "rm -rf dist && mkdir -p dist && cp -R _headers static *.html dist/"
  publish = "dist"
